document.getElementById('loginForm').addEventListener('submit',async e=>{e.preventDefault();try{const data=await api('/auth/contractor/login',{method:'POST',body:JSON.stringify({contractorCode:contractorCode.value.trim(),pin:pin.value})});localStorage.setItem('contractor_token',data.token);localStorage.setItem('contractor_user',JSON.stringify(data.user));location.href='contractor-form.html'}catch(err){show('msg',err.message)}});
